<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:foreground="@drawable/app_item_background"
              android:layout_width="85dp"
              android:layout_height="115dp"
              android:padding="10dp"
              android:orientation="vertical" android:id="@+id/appLayout"
              android:focusable="true"
              android:clickable="true"
              android:layout_marginTop="10dp">

    <ImageView
            android:layout_width="50dp"
            android:layout_height="50dp" app:srcCompat="@mipmap/ic_launcher" android:id="@+id/iconView"
            android:layout_weight="1" android:layout_gravity="center"/>
    <TextView
            android:text="@string/app_name"
            style="@style/TextAppearance.AppCompat.Small"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/appNameTv"
            android:layout_weight="1"
            android:maxLines="2"
            android:ellipsize="end"
            android:textColor="@android:color/white"
            android:textAlignment="center"
            android:layout_marginTop="15dp"
            android:layout_gravity="center" android:textSize="12sp"/>
</LinearLayout>